# bench/users_validator.py

def load_valid_users(json_path, *, log_path=None):
    """
    Заглушка: сюда вставляется решение модели.

    Ожидаемый API:
    load_valid_users(json_path, *, log_path=None) -> list[dict]

    - Читает JSON-файл.
    - Валидирует объекты (id:int, name:str, email:str).
    - Ошибки пишет в errors.log (или в log_path).
    - Возвращает список валидных пользователей.
    """
    raise NotImplementedError("Здесь должно быть решение модели")